"""Renderer implementations of the bi_terminal.renderers.base.Renderer protocol.

One subpackage per target: textual (the local rich TUI), ansi/petscii/atascii
(Synchronet BBS doors — stubs until built, see README "Sequencing").
"""
